let myInfo = {
    name: 'Rakin',
    age: 25,
    location: 'Toronto'
}

console.log(`${myInfo.name} is ${myInfo.age} and lives in ${myInfo.location}`)

myInfo.age = myInfo.age + 1

console.log(`${myInfo.name} is ${myInfo.age} and lives in ${myInfo.location}`)
